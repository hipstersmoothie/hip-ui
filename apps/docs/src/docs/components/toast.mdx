---
title: Toast
description: A Toast displays a brief, temporary notification of actions, errors, or other events in an application.
---

import { PropDocs } from '../../lib/PropDocs'
import { Example } from '../../lib/Example'
import { Basic } from '../../examples/toast/basic'
import { Actions } from '../../examples/toast/actions'
import { Icons } from '../../examples/toast/icons'

<Example src={Basic} />

## Installation

Run the following command to add the toast component to your project.

```bash
pnpm hip install toast
```

## Props

This component is built using [React Aria Toast](https://react-spectrum.adobe.com/react-aria/Toast.html).

<PropDocs components={["ToastRegion", "Toast", "ToastContent", "ToastTitle", "ToastDescription", "ToastClose"]} />

## Features

### Actions

Toasts can include an action button to perform an action when the user clicks on it.

<Example src={Actions} />

### Variants and Icons

Toasts can be one of the following variants:

- `success`
- `warning`
- `critical`

Each variant can include an icon to display alongside the title and description.

<Example src={Icons} />

## Related Components

- [Dialog](/docs/components/dialog) - For modal dialogs
- [AlertDialog](/docs/components/alert-dialog) - For critical actions and confirmations
- [Popover](/docs/components/popover) - For non-modal overlays
- [Button](/docs/components/button) - For triggering toasts

