---
title: Aspect Ratio
description: A component that allows you to set the aspect ratio of a div, with support for images.
---

import { Example } from '../../lib/Example'
import { Basic } from '../../examples/aspect-ratio/basic'
import { NoImage } from '../../examples/aspect-ratio/no-image'

<Example src={Basic}  />

## No Image

The aspect ratio can be set without an image.

<Example src={NoImage}  />